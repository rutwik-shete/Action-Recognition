Completed the dataset extractor . create data loader now

libraries to install:
    pip3 install pandas
    pip3 install fast_ml
    pip3 install scipy
    pip3 install matplotlib
    pip3 install seaborn
    pip3 install ipython
    pip3 install tqdm
    pip3 install scikit-learn
    pip3 install transformers
    pip3 install IProgress

How big TimeSFormer is

Shiftting the number of frams and size of the image

is it(TimeSFormer) trained with labels or without labels ?

Create Check points
Code crashed due to Batch side of the Validation set maybe
